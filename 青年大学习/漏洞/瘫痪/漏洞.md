## CVE-2026-49975

条件：开启了http2，并使用了未针对于该漏洞修复的header引用补丁的http服务器（nginx、apache）

原理：利用http2的头部字段的服务器复用，http2在后续的请求的header里的字段，如果之前传过，就会使用索引去复用，所以可以第一次传一个特别大的header，然后后面发一个很小的http2请求包，该请求包里添加非常多个该引用，导致服务器那边针对每个重复引用都释放内存，然后将攻击者将初始窗口大小设置成0，导致http响应无法回应，从而导致服务器卡死